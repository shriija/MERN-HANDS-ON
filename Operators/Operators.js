//Initial data:
        let totalAmount = 0;

//🎯 Tasks
  //1. Add ₹500 to the total
  let finallBill = totalAmount + 500
  //2. Add ₹1200 to the total
  finallBill += 1200
  //3. Apply a ₹200 discount
  finallBill -= 200
  //4. Add 18% GST
  finallBill *= 1.18
  //5. Print the final bill amount
  console.log("Final Bill Amount:" , finallBill)